<nav class="navbar navbar-expand px-3 bg-light">
  <!-- Left-aligned: Logo -->
  <div class="col-4 d-flex justify-content-start">
      <img src="/images/logos/logoSVG.svg" class="img-fluid" alt="" style="max-width: 100px;">
  </div>

  <!-- Center-aligned: Navigation Links -->
  <div class="col-4 navbar-collapse justify-content-center" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link nav-link-ltr" href="/">Home</a>
      </li>
      <% if (!user) { %>
          <li class="nav-item">
              <a class="nav-link nav-link-ltr" href="/login">Login</a>
          </li>
          <li class="nav-item">
              <a class="nav-link nav-link-ltr" href="/register">Register</a>
          </li>
      <% } else { %>
          <li class="nav-item">
              <a class="nav-link nav-link-ltr" href="/service">Log Service</a>
          </li>
          <li class="nav-item">
              <a class="nav-link nav-link-ltr red" href="/logout">Logout</a>
          </li>
      <% } %>
      </ul>
  </div>

  <!-- Right-aligned: User Information -->
  <div class="col-4 d-flex justify-content-end">
      <% if(user) { %>
          <span class="navbar-text">Current User: <span class="special"><%= user.username %><span></span>
      <% } %>
  </div>
</nav>

